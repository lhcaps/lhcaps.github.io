---
scope: Le Huy Systems Atlas frontend, verification, and static release architecture
purpose: build-substrate
altitude: feature
updated: 2026-08-23T01:02
---

- (constraint) Inherit SPEC-le-huy-systems-atlas CAP-1 through CAP-7 and every listed companion; repository truth, public-safe evidence, accessibility, privacy, and verification outrank visual preference.
- (constraint) Brownfield baseline is a React/Vite static GitHub Pages app with duplicate data, two Canvas paths, remote fonts, perpetual scene motion, a case-sensitive build defect, and an incomplete deployment gate; replacement is authorized without compatibility preservation for dead page abstractions.
- (version) Reality-checked from npm ls and package lock: Node 22.23.1 local, npm 11.12.1, React 19.2.6, React DOM 19.2.6, Vite 8.0.12, TypeScript 6.0.3, Three 0.184.0, React Three Fiber 9.6.1, Drei 10.7.7, Framer Motion 11.18.2, Tailwind 3.4.19, Vitest 4.1.8, Playwright 1.60.0.
- (version) Reality-checked from current npm registry and official Fontsource installation pages: Geologica Variable 5.3.0, Fragment Mono 5.3.0, axe-core Playwright 4.13.0, pdf-parse 2.4.5, tsx 4.23.12, Vitest coverage-v8 4.1.8.
- (decision) AD-1 uses a functional core with progressive-enhancement adapters: typed content, topology, Claim manifest, eligibility, and validators are pure modules; React DOM is the authoritative shell; WebGL, browser APIs, PDF extraction, and release I/O are adapters.
- (decision) AD-2 one immutable typed content graph owns Systems, nodes, routes, scene slots, Claim instances, narratives, anchors, contact facts, and capability links; selector, DOM topology, scene, metadata, CV source, and validators consume it without duplicate project datasets.
- (decision) AD-3 React owns selectedSystemId only; eligibility is derived from matchMedia width, reduced-motion, safe WebGL probe, prior scene failure, and first viewport entry; the scene owns no public state, content, navigation, or independent selection.
- (decision) AD-4 Canvas eligibility is width at least 768, Reduced Motion false, WebGL supported, no scene failure, and Atlas entered viewport; when eligibility turns false Canvas unmounts while selectedSystemId persists; offscreen after entry remains mounted with demand rendering and no invalidation until state changes.
- (decision) AD-5 scene choreography is a finite renderer-local reconfigure then handoff trace then settled sequence; useFrame mutates refs only and calls invalidate only until settled, rapid reselection retargets current values, and React state is never set inside the frame loop.
- (decision) AD-6 Public Claim Manifest owns exact Claim text, class, public-safe status, and limitation; unique claimInstanceId values may reuse evidenceKey values, and build validators reject overrides, unsafe classes, missing limitations, topology drift, private artifacts, and unsupported copy.
- (decision) AD-7 privacy validation separates safe policy category labels from unsafe values: allowlisted public identities, generic secret patterns, anonymous-booking fixtures, tracked/build artifact scanning, and an off-repo confidentiality review whose output contains only scope and pass or fail.
- (decision) AD-8 styling is one root token layer from DESIGN.md, Geologica Variable and Fragment Mono packaged locally, Tailwind only for stable utilities, and section-specific CSS classes for authored compositions; no remote font, CSS-in-JS, competing theme layer, or generic card abstraction.
- (decision) AD-9 semantic HTML owns headings, landmarks, navigation, selector buttons, readable topology, Claims, and links; mobile navigation is a focus-managed disclosure; Canvas is aria-hidden, unfocusable, pointer-inert, and wrapped by a contained error boundary.
- (decision) AD-10 performance budgets use per-file gzip level 9 and transitive Vite manifest closure: eager JavaScript at most 170 KiB gzip, lazy Atlas JavaScript at most 425 KiB gzip, CSS at most 25 KiB gzip, first-view Latin fonts at most 220 KiB transferred, initial transfer at most 450 KiB excluding lazy Atlas and CV, each non-font static asset at most 256 KiB, CV at most 512 KiB; no GLB, texture, raster hero, or post-processing asset is allowed.
- (decision) AD-11 npm run verify is the sole acceptance orchestrator and always writes schema-v1 artifacts/release/release-evidence.v1.json plus asset-inventory.v1.json before returning; it composes private/source scan, content/topology validation, typecheck, lint, coverage tests, build, budget/output validation, Playwright accessibility/E2E/matrix, and reviewer evidence.
- (decision) AD-12 Vite emits release.json from VITE_COMMIT_SHA or git HEAD without dirtying source; GitHub Pages uses Node 22.23.1, npm ci, Chromium install, npm run verify before upload, always uploads release evidence, and deploys only the verified dist; attestation compares workflow, deployment, remote, and public full SHA.
- (decision) AD-13 test seams are pure data/schema/eligibility/motion-policy units, React selector/navigation/fallback components, and Playwright user journeys; content fixtures prove valid Claim reuse, invalid overrides, safe Evidence Boundary labels, fake private values, route resolution, Form preview branch, and scene bypass/failure.
- (decision) AD-14 CV is a generated one-page facts-only static PDF at public/le-huy-software-engineer-cv.pdf, validated by pdf-parse and link annotations; generation input is the same public content graph, while the committed PDF is verified rather than rebuilt during every production build.
- (decision) AD-15 errors remain local: invalid content stops prebuild, scene import or WebGL failure sets one sticky sceneFailure and uses DOM topology, external link failures fail verification, and no runtime retry loop, error telemetry, backend, or client storage is introduced.
- (decision) AD-16 repository governance isolates generated release evidence and Playwright output from tracked source, preserves private ignored paths, stages exact files, sets the required local identity, and runs the exact-trailer history rewrite only after a verified external bundle and mechanical tree and identity equivalence.
- (decision) AD-17 dependencies are directionally contained: core content and validators import no React or Three; DOM components may import core; the lazy scene adapter may import core plus Three/R3F/Drei; no eager module imports the scene adapter or Three transitively.
- (decision) AD-18 sceneSlot coordinates are a single architecture-owned map in the lazy Atlas adapter; slots map to fixed normalized x/y/z positions, route geometry resolves only from node IDs, and no System-specific hard-coded geometry exists outside the authoritative data graph.
- (direction) Alternatives rejected: CMS or runtime API for static verified content, Canvas-owned selection, capable-device mobile exception, always frameloop, OrbitControls, duplicated scene fixtures, runtime access to private evidence, remote fonts, full-page snapshot acceptance, and a second visual system.
- (event) Architecture decisions complete in memlog; distill the feature-altitude spine, run deterministic lint, then the full configured reviewer gate.
- (decision) Supersedes ambiguous scene-slot ownership wording: the content graph owns each node semantic sceneSlot assignment from topology-contract.md; the lazy scene adapter owns only the single sceneSlot-to-coordinate map and may not infer or override assignments.
- (constraint) Critical 100 percent per-file coverage set is closed to src/content/validate.ts plus src/atlas/core/claims.ts, eligibility.ts, motion.ts, sceneSlots.ts, and topology.ts; project floors remain 85 percent lines/functions/statements and 80 percent branches.
- (constraint) Source public assets are allowlisted to favicon.svg, og-image.png, robots.txt, sitemap.xml, and le-huy-software-engineer-cv.pdf; remove public/credentials entirely, and Vite alone emits release.json.
- (decision) PublicReleaseIdentityV1 is a closed five-field object with schemaVersion, full sha, repository, ref, and canonicalUrl; it never derives from or copies internal Release Evidence records.
- (decision) sceneStatus is Atlas-adapter ephemeral state untried/loading/ready/failed-sticky; failure resets only on full document reload and no resize, preference, re-entry, or reselection may retry a failed scene.
- (decision) CV provenance uses deterministic pdf-lib regeneration, canonical public-graph digest, generator version, byte comparison, pdf-parse text extraction, and pdf-lib/raw URI annotation checks; a non-sensitive digest-bound confidentiality receipt is verified but never deployed.
- (decision) Budget traversal assigns shared chunks to eager only, Atlas closure subtracts eager, unclassified dynamic roots fail, Vite manifest and chunk module inventory are mandatory, and fixed graph fixtures test traversal.
- (version) Current primary Pages guidance selected checkout v6, setup-node v6, configure-pages v5, upload-pages-artifact v4, deploy-pages v4, and upload-artifact v4 for the implementation workflow.
- (event) Applied clear fixes from good-spine, current-tech, and adversarial reviews; remaining repository package, script, workflow, and Vite deltas are explicit first-story implementation gates, not unverified technology choices.
- (constraint) Clarifies the exact critical coverage set with full paths: src/content/validate.ts and src/atlas/core/claims.ts, eligibility.ts, motion.ts, sceneSlots.ts, and topology.ts, all under their explicit directory prefixes in the spine.
- (event) Reviewer Gate passed: deterministic lint zero findings; good-spine, current-technology, and adversarial-seam reviewers all passed with zero remaining severities; spine finalized.
- (decision) Public Verification Harness is explanatory and privacy-minimal: it never links or copies internal Release Evidence, may read only closed PublicReleaseIdentityV1, and never publishes VERIFIED_COMPLETE; current completion remains an internal/final handoff assertion after hosted gates.
- (decision) Predeploy reviewer acceptance uses a tracked non-deployed schema-v1 index bound to a deterministic release-source digest; exact required lenses must be pass with no unresolved findings. Production screenshot review is a separate hosted attestation bound to workflow SHA and deployed public identity.
- (decision) Repository confidentiality adds a fresh safe pattern-based audit of every commit and tree reachable from candidate HEAD plus generated evidence outputs; it emits a schema-v1 internal receipt bound to full HEAD SHA and fails without using private-ledger values.
- (decision) Confidentiality review digest is SHA-256 over canonical UTF-8 graph JSON plus sorted normalized shipped-asset path/length/raw-byte records, excluding release.json and the receipt; no wall-clock expiry, and stale means content mismatch, malformed receipt, non-pass result, or future review time.
- (decision) Pages provenance trusts the immutable workflow SHA, uploaded artifact ID, and deterministic dist digest, then cache-busted deployed release.json; all identity fields and digests must match. If main advances before attestation, the run is PARTIAL and the newer head must complete its own run.
- (decision) Every Active Handoff has a required destination focus node equal to the route destination; the Form topology keeps Temporary Preview as a terminal branch and only Persisted Document routes to Audit and Verification.
- (decision) Resolve review-attestation commit circularity by keeping safe reviewer reports tracked and binding them to the exact release-source digest, while npm run verify generates an ignored ReviewAttestationIndexV1 after clean checkout with full sourceSha and sourceTree repeated at root and per review; CI therefore consumes exact-candidate attestations without embedding a commit SHA inside that commit.
- (decision) Review-source digest uses a closed release-source path policy and FileRecordV1 byte encoder; tracked reports live only under docs/release/reviews, are excluded from the source digest to avoid self-reference, carry that digest in safe frontmatter, and are hashed individually into the generated exact-SHA/tree index. Untracked, modified, symlinked, or escaping evidence fails.
- (decision) Pages workflow grants actions:read, exports the sole github-pages artifact ID, verifies its REST record at the exact repository artifact endpoint for name/run/head/expiry/digest, downloads and safely rehashes the immutable artifact contents, then deploy-pages consumes that exact exported name from the dependent job.
- (decision) All release digests share an exact FileRecordV1 encoder: NFC UTF-8 slash-normalized relative path, NUL, ASCII byte length, NUL, raw bytes, NUL; paths with NUL, backslash, dot segments, non-NFC, symlinks, or non-files fail. DIST-V1 includes every dist file including release.json and rejects source maps/internal evidence.
- (decision) CanonicalJsonV1 restricts content data to null, booleans, strings, safe integers, arrays, and objects; object keys sort by UTF-16 code units, arrays retain order, and JSON string escaping is exact. Float, nonfinite, negative-zero, unsafe integer, lone-surrogate, and unsupported values fail with test vectors.
- (decision) History scan rules are tracked at docs/release/history-safe-patterns.v1.json with a closed schema for generic commit-message/path/text rules, binary policy, exact allowed Cursor trailer, and ordered generated evidence roots; logs and receipts expose rule IDs and counts only.
- (decision) Every Pages or attestation job that verifies history uses checkout v6 with fetch-depth 0 and asserts a non-shallow repository before npm run verify.
- (decision) Separate Vite build output from Pages upload: verify dist including internal dist/.vite/manifest.json, then stage only validated public regular files into ignored artifacts/pages-site with no dotfiles or links. PAGES-SITE-V1 digests that exact staging root, and upload-pages-artifact receives it; the Vite manifest never deploys.
- (event) Final reviewer gate passed on 2026-08-23: Node 24 action pins migrated and verified live; architecture lint has zero findings; good-spine, current-technology, and adversarial-seam reviews report P0/P1/P2 zero; spine status finalized.
- (decision) Hosted browser verification supersedes the discovery-era Three r184 candidate: the final architecture pins Three and its types to 0.182.0 with R3F 9.6.1, avoiding the `THREE.Clock` deprecation warning introduced in r183+ while preserving the verified scene contract; unused Drei and Framer Motion direct dependencies are removed.
